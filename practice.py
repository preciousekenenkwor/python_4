#create a calculator using basic input like 
#this from the user:1 +1 should
#return the computed value in the console 
#caveat:the input must be just one single input
#Hint.use the split to separate the input
# and get the computation

m = ('enter the numbers 1+1 :')
m = int(m)

using loops create a loop where a user will input
#(10) different values and you will print all the user's 
#input in an arranged order ie alpabetical order.if its is an 
#integar input you should create a list for integer input and 
#aphabet input,and ,make sure the values 
#values in the list are unique meaning no word or value should 
#repeat itself for example:user derek 


#using a for loop with a list that seperates negative 
#from positive
Eso = [3, 2, 4, 6, 5, -6, -6, -7, -9]
for i in Eso:
    if i > 0:
        negative.append()
    else:
       

 